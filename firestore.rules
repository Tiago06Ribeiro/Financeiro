// Cole isso no Firebase Console → Firestore → Rules → Publicar

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Só usuários autenticados acessam os dados compartilhados
    match /appdata/{document} {
      allow read, write: if request.auth != null;
    }
  }
}
